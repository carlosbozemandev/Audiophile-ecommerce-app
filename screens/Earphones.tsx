import Category from "../components/Category";

export default function Earphones({ navigation }: any): JSX.Element {
    return (
        <Category cat={'earphones'} navigation={navigation} />
    );
}